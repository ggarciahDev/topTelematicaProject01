# Instalación

* En la carpeta topTelematicaProject01, ejecutar:

  `$ npm install`
  
* En la carpeta topTelematicaProject01/frontend, ejecutar:

  `$ npm install`
  
  `$ npm start`
  
# Acerca del proyecto
